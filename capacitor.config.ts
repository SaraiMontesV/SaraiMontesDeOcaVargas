import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'sarai-montes-de-oca-vargas',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
